"""Volatility-richness analytics (VRP, term/skew, regime gate).

Descriptor-first per FlashAlpha rule 4: these modules rank IV rich/cheap vs a
forward RV forecast but emit NO signals. Promotion to ``strategies/`` happens
only after the backtest proves edge.
"""
